FILE(GLOB SRC */*.cpp)
FILE(GLOB HEADERS */*.h */*.inl *.h *.inl)

IF (${CMAKE_SYSTEM_NAME} MATCHES "Windows")
	ADD_DEFINITIONS (-DWM5_CORE_DLL_EXPORT=1)
ENDIF ()

INCLUDE_DIRECTORIES(${CORE_INCLUDES})

ADD_LIBRARY (Wm5Core SHARED ${SRC} ${HEADERS})